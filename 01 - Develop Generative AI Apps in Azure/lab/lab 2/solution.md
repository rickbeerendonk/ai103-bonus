# Lab 2 - Solution

4. The Responses API exposes `response.output_text`.
5. The application appends the complete messages array and sends it on every request.
6. Use the previous response or conversation identifier (`response.id`) stored in f.e. `previous_response_id`.
