# Lab 1 - Solution

1. A project groups models, deployments, tools, knowledge, and application assets.
2. A model is the trained artifact; a deployment is its named callable instance with quota and configuration.
3. Use the Azure OpenAI/OpenAI endpoint for direct model calls; use the project endpoint for agent-level abstractions.
